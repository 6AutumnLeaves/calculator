




const mathFunctions = {
add: (a, b) => (a + b).toFixed(2),
subtract: (a, b) => (a - b).toFixed(2),
multiply: (a, b) => (a * b).toFixed(5),
divide: (a, b) => (a / b).toFixed(5),

};
const operate = function(operator, a, b) {

}